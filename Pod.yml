apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    app: myapp
    type: frontend
spec:
  containers:
    - name: nginx-container
      image: nginx:latest


# kubectl create -f pod.yml
# kubectl get pod
# kubectl get pods -o wide
# kubectl describe pod myapp-pod
